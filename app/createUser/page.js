import React from 'react';
import UserForm from '../(components)/userForm';

const CreateUser = () => {
    return (
        <div>
      <UserForm></UserForm>
        </div>
    );
};

export default CreateUser;